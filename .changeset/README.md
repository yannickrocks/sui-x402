# Changesets

Versioning and changelogs for the publishable packages (`@sui-x402/*`). Examples
are ignored. `pnpm changeset` adds an entry; `pnpm changeset version` bumps and
writes changelogs; publishing is a human step (`pnpm changeset publish`), never
run by the agent.
